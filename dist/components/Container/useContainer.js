import { makeStyles } from "@mui/styles";
export var useContainer = makeStyles(function (theme) { return ({
    root: {
        maxWidth: theme.eleganteStyles.maxWidth,
        width: "85%",
        margin: "0 auto",
    },
}); });
